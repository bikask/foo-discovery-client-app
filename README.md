# foo-discovery-client-app
* this service uses the discovery service to discover other services. 
* and then use a discovery client to use access that
* let spring resolve the uri of other microserive
